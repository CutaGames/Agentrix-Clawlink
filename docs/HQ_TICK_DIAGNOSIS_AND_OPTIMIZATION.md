# HQ 自动执行系统诊断报告与优化方案

> 诊断时间: 2026-02-11 | 服务器: 57.182.89.146 (东京)

---

## 一、系统架构概览

```
┌─────────────────────────────────────────────────────────┐
│                    Tick Service (每10分钟)                │
│  Cron → 获取任务 → 分配Agent → 触发AI → 保存结果        │
├─────────────────────────────────────────────────────────┤
│  AutoTaskGenerator    → 为空闲Agent自动生成任务           │
│  AgentTrigger         → 调用 UnifiedChatService          │
│  BudgetMonitor        → 预算追踪 (免费Agent=$0)          │
│  AgentMetrics         → 失败退避 + 自动恢复              │
│  AgentLearning        → 任务学习 + 知识共享              │
├─────────────────────────────────────────────────────────┤
│  UnifiedChatService   → 构建prompt + 工具循环(最多5轮)    │
│  HqAIService          → Gemini/Groq/Bedrock 多provider   │
│  ToolService          → 16个工具 (web_search, twitter等)  │
└─────────────────────────────────────────────────────────┘
```

**13个Agent**, 全部映射到 Gemini (代码中 `agentAIMapping`):
- COMMANDER-01, ANALYST-01 → `gemini-1.5-pro` (50次/天/key)
- 其余11个 → `gemini-2.0-flash` (1500次/天/key)

---

## 二、失败原因诊断

### 2.1 数据总览

| 指标 | 数值 |
|------|------|
| 总任务数 | 425 |
| 成功 | 286 (67.3%) |
| 失败 | 138 (32.5%) |
| 待处理 | 1 |

### 2.2 失败原因分类

| 错误类型 | 数量 | 占比 |
|----------|------|------|
| **Gemini 429 Too Many Requests (配额耗尽)** | 72 | **52.2%** |
| **Request failed with status code 403** | 32 | **23.2%** |
| **404 gemini-2.0-flash does not exist** | 17 | 12.3% |
| **404 gemini-1.5-flash does not exist** | 17 | 12.3% |

### 2.3 根因分析

#### 🔴 根因 #1: RPM (每分钟请求数) 超限 — 占52%

**Gemini 免费额度的关键限制不是 RPD (每天请求数)，而是 RPM (每分钟请求数)！**

```
gemini-2.0-flash: RPM = 15次/分钟/key × 3个key = 45次/分钟
gemini-1.5-pro:   RPM = 2次/分钟/key × 3个key = 6次/分钟
```

当前 Tick 系统每10分钟触发一次，一次处理最多12个任务。
每个任务可能触发1-5轮工具调用循环，每轮都是一次API请求。

**实际情况**: 一个Tick内，12个任务 × 平均2轮 = ~24次请求，在几秒内并发发出。
这远超 RPM=15 的限制，导致大量 429 错误。

#### 🔴 根因 #2: Gemini API key 只配了1个 — 占23%

服务器 `.env` 中只有 `GEMINI_API_KEY` 一个变量（虽然日志显示3个key，但可能是逗号分隔的同一变量）。
`GEMINI_API_KEY1` 和 `GEMINI_API_KEY2` 未配置，导致轮换不充分。

403 错误通常是 key 无效或被临时封禁（频繁触发429后的惩罚）。

#### 🔴 根因 #3: 模型名称不匹配 — 占24%

`404 gemini-2.0-flash does not exist` 和 `404 gemini-1.5-flash does not exist`
说明部分请求使用了错误的模型名称。正确的模型ID应该是:
- `gemini-2.0-flash` (部分地区可能需要 `gemini-2.0-flash-001`)
- `gemini-1.5-flash` → 可能需要 `gemini-1.5-flash-latest` 或 `gemini-1.5-flash-002`

#### 🟡 根因 #4: 无请求间隔控制

`agent-trigger.service.ts` 中 `triggerMultipleAgents` 有2秒间隔，
但 `tick.service.ts` 的主循环 `executeTask` 是串行但无间隔的。
12个任务连续执行，每个任务内还有最多5轮工具调用，全部无间隔。

### 2.4 时间线分析

```
2/8:  88 成功, 0 失败  ← 正常（请求量低）
2/9:  142 成功, 38 失败 ← 开始出现429
2/10: 56 成功, 100 失败 ← 大量429，从15:00开始全部失败
2/11: 0 成功, 全部失败  ← 3个key全部耗尽
```

从2/10 15:00 UTC 开始，连续9小时（15:00-20:00+）全部失败，
说明 RPD 配额也被耗尽（每天重置时间是太平洋时间午夜 = UTC 08:00）。

---

## 三、工具调用能力验证

### 3.1 已注册工具 (16个)

| 工具 | 类型 | 依赖 | 状态 |
|------|------|------|------|
| `read_file` | 文件 | 本地文件系统 | ✅ 可用 |
| `write_file` | 文件 | 本地文件系统 | ✅ 可用 |
| `edit_file` | 文件 | 本地文件系统 | ✅ 可用 |
| `search_files` | 文件 | 本地文件系统 | ✅ 可用 |
| `list_directory` | 文件 | 本地文件系统 | ✅ 可用 |
| `run_shell` | 系统 | 本地shell | ✅ 可用 |
| `http_request` | 网络 | 无依赖 | ✅ 可用 |
| `web_search` | 搜索 | GOOGLE_SEARCH_API_KEY 或 SearXNG | ⚠️ 需验证key |
| `twitter_post` | 社交 | TWITTER_API_KEY 等 | ⚠️ 需验证key |
| `twitter_search` | 社交 | TWITTER_API_KEY | ⚠️ 需验证key |
| `twitter_engage` | 社交 | TWITTER_API_KEY | ⚠️ 需验证key |
| `discord_send` | 社交 | DISCORD_BOT_TOKEN | ⚠️ 需验证key |
| `send_email` | 邮件 | SMTP配置 | ⚠️ 需验证 |
| `github` | 开发 | GITHUB_TOKEN | ⚠️ 需验证key |
| `social_publish` | 社交 | 多平台token | ⚠️ 需验证 |
| `telegram_send` | 社交 | TELEGRAM_BOT_TOKEN | ⚠️ 需验证key |

### 3.2 关键问题

1. **工具虽然注册了，但大部分社交工具依赖 env 变量中的 API key**
2. **即使 Agent 决定调用工具，如果对应的 API key 未配置，工具会返回错误或 dry-run**
3. **web_search 依赖 `GOOGLE_SEARCH_API_KEY` + `GOOGLE_SEARCH_CX`，否则 fallback 到 SearXNG**
4. **Gemini 的 function calling 本身工作正常（日志显示 "11 tools available"），但 429 导致连 AI 调用都失败了，根本到不了工具执行阶段**

---

## 四、优化方案

### 方案 A: 请求速率控制 (最关键 — 解决52%失败)

**核心思路**: 遵守 RPM=15/key 的限制，在请求间加入延迟。

```typescript
// tick.service.ts - executeTask 之间加入间隔
for (const task of executableTasks) {
  // ... 执行任务 ...
  await this.executeTask(agent, task);
  
  // 🆕 关键: 每个任务执行后等待4秒，确保不超RPM
  await this.sleep(4000);
}
```

```typescript
// hq-ai.service.ts - Gemini 调用前加入 RPM 限流器
private geminiRateLimiter = {
  lastCallTime: new Map<number, number>(), // keyIndex → timestamp
  minIntervalMs: 4200, // 60s / 15rpm ≈ 4s per request per key
};

private async waitForRateLimit(keyIndex: number): Promise<void> {
  const last = this.geminiRateLimiter.lastCallTime.get(keyIndex) || 0;
  const elapsed = Date.now() - last;
  if (elapsed < this.geminiRateLimiter.minIntervalMs) {
    await new Promise(r => setTimeout(r, this.geminiRateLimiter.minIntervalMs - elapsed));
  }
  this.geminiRateLimiter.lastCallTime.set(keyIndex, Date.now());
}
```

### 方案 B: 纯 Gemini 多模型分散 (解决24%失败 + 3倍容量)

**策略**: 全部使用 Gemini，3个API key × 4种模型，每种模型有独立配额。

**Agent-模型映射**:

| Agent | Model | 单key RPD | 3key RPD | RPM |
|-------|-------|-----------|----------|-----|
| COMMANDER-01 | gemini-1.5-pro | 50 | 150 | 2×3=6 |
| ANALYST-01 | gemini-2.0-flash | 1500 | 4500 | 15×3=45 |
| REVENUE-01 | gemini-2.0-flash | ↑共享 | ↑共享 | ↑共享 |
| GROWTH-01 | gemini-2.0-flash | ↑共享 | ↑共享 | ↑共享 |
| BD-01 | gemini-2.0-flash | ↑共享 | ↑共享 | ↑共享 |
| SOCIAL-01 | gemini-1.5-flash | 1500 | 4500 | 15×3=45 |
| CONTENT-01 | gemini-1.5-flash | ↑共享 | ↑共享 | ↑共享 |
| DEVREL-01 | gemini-1.5-flash | ↑共享 | ↑共享 | ↑共享 |
| SUPPORT-01 | gemini-1.5-flash | ↑共享 | ↑共享 | ↑共享 |
| SECURITY-01 | gemini-1.5-flash-8b | 1500 | 4500 | 15×3=45 |
| LEGAL-01 | gemini-1.5-flash-8b | ↑共享 | ↑共享 | ↑共享 |
| ARCHITECT-01 | gemini-1.5-flash-8b | ↑共享 | ↑共享 | ↑共享 |
| CODER-01 | gemini-1.5-flash-8b | ↑共享 | ↑共享 | ↑共享 |

**总容量**: 3 keys × (1500+1500+1500+50) = **13,650次/天**

**降级链**: 当某模型配额耗尽时，自动降级到其他模型:
- `gemini-2.0-flash → gemini-1.5-flash → gemini-1.5-flash-8b`
- `gemini-1.5-pro → gemini-2.0-flash → gemini-1.5-flash → gemini-1.5-flash-8b`

**扩展性**: 后续可随时增加更多免费 Gemini API key，每增加1个key = +4,550次/天

### 方案 C: 智能调度 (最大化利用率)

```
每日可用请求预算 (纯Gemini):
- Gemini 2.0 Flash:    1500×3 = 4500/天, 15×3 = 45/分钟
- Gemini 1.5 Pro:        50×3 = 150/天,   2×3 = 6/分钟  
- Gemini 1.5 Flash:    1500×3 = 4500/天, 15×3 = 45/分钟
- Gemini 1.5 Flash-8B: 1500×3 = 4500/天, 15×3 = 45/分钟

总计: 13,650次/天 (可通过增加API key线性扩展)
```

**调度策略**:
1. **Tick 间隔从10分钟改为15分钟** — 减少并发压力
2. **每个Tick最多处理6个任务** (而非12个) — 控制RPM
3. **任务间隔4秒** — 确保不超RPM
4. **模型降级链**: gemini-2.0-flash → gemini-1.5-flash → gemini-1.5-flash-8b (纯Gemini)
5. **429错误后等待60秒再重试** (而非立即尝试下一个key)

### 方案 D: 多模型利用 (充分利用所有免费模型)

当前只用了 `gemini-2.0-flash` 和 `gemini-1.5-pro`。
还有 `gemini-1.5-flash` 和 `gemini-1.5-flash-8b` 完全没用！

每个模型有独立的配额:
```
gemini-2.0-flash:   1500 RPD × 3 keys = 4500
gemini-1.5-flash:   1500 RPD × 3 keys = 4500  ← 未使用！
gemini-1.5-flash-8b: 1500 RPD × 3 keys = 4500 ← 未使用！
gemini-1.5-pro:     50 RPD × 3 keys = 150
```

**如果全部利用**: 4500 + 4500 + 4500 + 150 = **13,650次/天** (仅Gemini)
加上 Groq: 13,650 + 14,400 = **28,050次/天**

**实现方式**: 在 `geminiChat` 中，当 gemini-2.0-flash 429时，不只是换key，还要换模型:
```
gemini-2.0-flash (key1) → gemini-2.0-flash (key2) → gemini-2.0-flash (key3)
→ gemini-1.5-flash (key1) → gemini-1.5-flash (key2) → gemini-1.5-flash (key3)
→ gemini-1.5-flash-8b (key1) → ... → Groq fallback
```

---

## 五、实施优先级

### P0 — 立即修复 (解决80%失败)

1. **添加请求间隔**: tick 任务间加4秒延迟
2. **确认3个Gemini key都正确配置**: `GEMINI_API_KEY`, `GEMINI_API_KEY1`, `GEMINI_API_KEY2`
3. **429后等待60秒**: 而非立即尝试下一个key

### P1 — 本周完成 (最大化免费资源)

4. **多模型降级链**: flash → 1.5-flash → 1.5-flash-8b (纯Gemini)
5. **13个Agent均匀分配到4种Gemini模型**: 避免单一模型过载
6. **Tick间隔改为15分钟**, 每Tick最多6个任务
7. **后续可增加更多免费Gemini API key**: 每增加1个key = +4,550次/天

### P2 — 下周完成 (工具能力验证)

7. **验证并配置所有社交工具的API key**
8. **添加 Google Search API key** (免费100次/天)
9. **添加 RPM/RPD 计数器到 BudgetMonitor**
10. **添加 Telegram/Discord 通知**: 当配额耗尽时通知

---

## 六、env 配置建议

```bash
# === Gemini (3个独立key，每个有独立配额) ===
GEMINI_API_KEY=key1_here
GEMINI_API_KEY1=key2_here
GEMINI_API_KEY2=key3_here

# === Groq (免费 14,400次/天) ===
GROQ_API_KEY=gsk_xxx

# === Web Search (Google Custom Search 免费100次/天) ===
GOOGLE_SEARCH_API_KEY=xxx
GOOGLE_SEARCH_CX=xxx

# === Twitter API (免费tier: 1500 tweets/月) ===
TWITTER_API_KEY=xxx
TWITTER_API_SECRET=xxx
TWITTER_ACCESS_TOKEN=xxx
TWITTER_ACCESS_SECRET=xxx

# === Telegram Bot ===
TELEGRAM_BOT_TOKEN=xxx
TELEGRAM_CHANNEL=@agentrix_channel

# === Discord Bot ===
DISCORD_BOT_TOKEN=xxx
DISCORD_ANNOUNCE_CHANNEL=channel_id

# === Email (Zoho 免费) ===
SMTP_HOST=smtp.zoho.com
SMTP_PORT=465
SMTP_USER=xxx
SMTP_PASS=xxx

# === GitHub ===
GITHUB_TOKEN=ghp_xxx
```

---

## 七、预期效果

| 指标 | 当前 | 优化后 |
|------|------|--------|
| 每日可用请求 | ~4,500 (仅2.0-flash) | ~13,650 (4种Gemini模型) |
| 任务成功率 | 67% | >95% |
| 每日完成任务 | ~56 | ~200+ |
| 7×24运行 | ❌ (15:00后全挂) | ✅ |
| 工具实际执行 | ❌ (429阻断) | ✅ |
| 扩展性 | 固定 | 每增加1个key = +4,550次/天 |
