# Agentrix Agent 支付场景全覆盖指南

> 本文档定义了 Agentrix 在 AI Agent 对话场景下支持的所有支付场景、对应的 MCP 工具和支付流程。

## 目录

1. [场景概览](#场景概览)
2. [用户身份场景](#用户身份场景)
3. [商品购买场景](#商品购买场景)
4. [订阅与计费场景](#订阅与计费场景)
5. [微支付场景](#微支付场景)
6. [钱包管理场景](#钱包管理场景)
7. [MCP 工具清单](#mcp-工具清单)
8. [支付方式支持](#支付方式支持)

---

## 场景概览

### 核心理念

**Product as Skill** - 商品即技能，让 AI Agent 能够直接调用商品/服务完成交易闭环。

### 用户类型

| 类型 | 说明 | 支付能力 |
|------|------|----------|
| **游客用户** | 无 Agentrix 账户，通过 ChatGPT/Claude 使用 | Guest Checkout（邮箱 + 支付链接）|
| **注册用户** | 有 AX ID，未设置授权 | 需要跳转确认支付 |
| **授权用户** | 有 AX ID + QuickPay/X402 授权 | 对话内直接扣款 |

---

## 用户身份场景

### 场景 1：对话内注册 + 创建钱包

**触发条件**：用户说"我想注册"/"创建钱包"/"开通账户"

**工具**：`create_wallet`

**流程**：
```
用户: "我想开通 Agentrix 账户"
     ↓
Agent: [create_wallet]
     ↓
系统: "请提供您的邮箱地址"
     ↓
用户: "test@example.com"
     ↓
Agent: [create_wallet email=test@example.com]
     ↓
返回: {
  walletAddress: "0x1234...",
  axId: "AX2024ABC",
  backupCode: "apple-banana-cherry-dragon-eagle-forest",
  message: "钱包创建成功！请保存备份码..."
}
```

### 场景 2：设置支付授权

**触发条件**：用户说"设置快速支付"/"开启自动支付"/"设置授权"

**工具**：`setup_agent_authorization`

**流程**：
```
用户: "我想设置自动支付，单笔最多 200 元"
     ↓
Agent: [setup_agent_authorization type=both singleLimit=200 dailyLimit=1000]
     ↓
返回: {
  success: true,
  message: "✅ 支付授权已设置！单笔限额: ¥200，每日限额: ¥1000",
  setupUrl: "https://agentrix.top/app/quickpay/setup" // 未登录时
}
```

---

## 商品购买场景

### 场景 3：一键购买（授权用户）

**触发条件**：已授权用户说"买这个"/"下单"/"购买"

**工具**：`quick_purchase` + `pay_with_wallet`

**流程**：
```
用户: "帮我买一个无线耳机"
     ↓
Agent: [search_products query="无线耳机"]
     ↓
返回: 商品列表
     ↓
用户: "买第一个"
     ↓
Agent: [quick_purchase productId=xxx quantity=1]
     ↓
系统: 检测到 QuickPay 授权且余额充足
     ↓
Agent: [pay_with_wallet amount=299 productId=xxx]
     ↓
返回: {
  success: true,
  transactionId: "tx_xxx",
  message: "✅ 支付成功！订单已创建。"
}
```

### 场景 4：游客购买（无账户）

**触发条件**：未注册用户在 ChatGPT 中购物

**工具**：`quick_purchase` + `collect_user_info`

**流程**：
```
用户: "我想买这个商品"
     ↓
Agent: [quick_purchase productId=xxx]
     ↓
返回: "请提供您的邮箱地址"
     ↓
用户: "guest@example.com"
     ↓
Agent: [collect_user_info infoType=email email=guest@example.com]
     ↓
Agent: [quick_purchase productId=xxx email=guest@example.com]
     ↓
返回: {
  success: true,
  checkoutUrl: "https://agentrix.top/checkout/pay?...",
  message: "👉 点击链接完成支付"
}
```

### 场景 5：两步确认购买

**触发条件**：用户希望先看总价再决定

**工具**：`prepare_checkout` → `confirm_payment`

**流程**：
```
用户: "我想买 3 件，先看看总价"
     ↓
Agent: [prepare_checkout productId=xxx quantity=3 email=xxx]
     ↓
返回: {
  intentId: "intent_xxx",
  totalPrice: 897,
  confirmationRequired: true,
  message: "商品: xxx x3，总价: ¥897。确认购买吗？"
}
     ↓
用户: "确认"
     ↓
Agent: [confirm_payment intentId=intent_xxx]
     ↓
返回: { success: true, checkoutUrl: "..." }
```

### 场景 6：实物商品（需收货地址）

**触发条件**：购买实物商品

**工具**：`collect_user_info` + `quick_purchase`

**流程**：
```
Agent: [quick_purchase productId=physical_xxx email=xxx]
     ↓
返回: "这是实物商品，请提供收货地址"
     ↓
用户: "张三，13800138000，北京市朝阳区xxx路123号"
     ↓
Agent: [collect_user_info infoType=shipping shippingAddress={...}]
     ↓
Agent: [quick_purchase productId=xxx email=xxx]
     ↓
返回: { success: true, checkoutUrl: "..." }
```

---

## 订阅与计费场景

### 场景 7：订阅 SaaS 服务

**触发条件**：用户说"订阅"/"开通会员"/"购买服务"

**工具**：`subscribe_service`

**流程**：
```
用户: "我想订阅 GPT-4 API 服务，按月付"
     ↓
Agent: [subscribe_service serviceId=gpt-4-api plan=monthly autoRenew=true]
     ↓
返回: {
  subscriptionId: "sub_xxx",
  setupUrl: "https://agentrix.top/subscribe/gpt-4-api?plan=monthly",
  message: "📋 订阅 GPT-4 API\n周期: 月付\n👉 点击链接完成订阅"
}
```

### 场景 8：API 按量计费

**触发条件**：调用付费 API 后结算

**工具**：`pay_api_usage`

**流程**：
```
// Agent 调用某付费 API 后
Agent: [pay_api_usage apiId=translation-api usage=1000 usageType=tokens]
     ↓
返回: {
  success: true,
  amount: 0.1,
  message: "✅ API 费用支付成功\n用量: 1000 tokens\n费用: 0.1 USDC"
}
```

### 场景 9：查询订阅状态

**触发条件**：用户说"我的订阅"/"查看订阅"

**工具**：`check_subscription_status`

**流程**：
```
用户: "我订阅了哪些服务？"
     ↓
Agent: [check_subscription_status]
     ↓
返回: {
  subscriptions: [...],
  totalMonthly: 35,
  message: "📋 订阅状态\n• GPT-4 API: ¥20/月\n• Claude API: ¥15/月"
}
```

---

## 微支付场景

### 场景 10：X402 协议支付（API 访问）

**触发条件**：访问需要 X402 支付的资源

**工具**：`x402_pay`

**流程**：
```
// Agent 遇到 402 Payment Required 响应
Agent: [x402_pay resourceUrl="https://api.example.com/premium" maxPrice=0.1]
     ↓
检查授权 → 检查余额 → 自动支付
     ↓
返回: {
  success: true,
  price: 0.01,
  protocol: "X402",
  message: "已自动支付 0.01 USDC，资源访问成功"
}
```

### 场景 11：X402 授权设置

**触发条件**：用户说"开启 X402"/"设置微支付"

**工具**：`x402_authorize`

**流程**：
```
用户: "开启自动微支付，每次最多 1 美元"
     ↓
Agent: [x402_authorize singleLimit=1 dailyLimit=10]
     ↓
返回: {
  success: true,
  x402Config: { singleLimit: 1, dailyLimit: 10 },
  message: "✅ X402 授权已设置\n单次限额: 1 USDC\n每日限额: 10 USDC"
}
```

---

## 钱包管理场景

### 场景 12：查询余额

**触发条件**：用户说"余额"/"还有多少钱"

**工具**：`get_wallet_balance`

**流程**：
```
用户: "我的余额是多少？"
     ↓
Agent: [get_wallet_balance]
     ↓
返回: {
  balance: { usdc: 100.50, eth: 0.05, cny: 720 },
  message: "💰 钱包余额\nUSDC: 100.50\nETH: 0.05\n≈ ¥720"
}
```

### 场景 13：充值（法币转加密）

**触发条件**：用户说"充值"/"存钱"/"买 USDC"

**工具**：`topup_wallet`

**流程**：
```
用户: "充值 100 元"
     ↓
Agent: [topup_wallet amount=100 currency=CNY method=stripe]
     ↓
返回: {
  checkoutUrl: "https://agentrix.top/topup?amount=100&currency=CNY",
  paymentMethods: ["stripe", "google_pay", "apple_pay", "alipay", "wechat_pay"],
  message: "💰 充值 100 CNY\n👉 点击链接完成充值\n支持: 信用卡/Google Pay/Apple Pay/支付宝/微信"
}
```

### 场景 14：直接钱包支付

**触发条件**：用户说"用钱包付"/"用余额付"

**工具**：`pay_with_wallet`

**流程**：
```
用户: "用钱包余额支付 50 USDC"
     ↓
Agent: [pay_with_wallet amount=50 currency=USDC description="xxx服务费"]
     ↓
检查授权 → 检查余额 → 执行支付
     ↓
返回: {
  success: true,
  transactionId: "tx_xxx",
  message: "✅ 支付成功！剩余余额: 50.50 USDC"
}
```

---

## MCP 工具清单

### 身份与钱包

| 工具 | 描述 | 必需参数 |
|------|------|----------|
| `create_wallet` | 创建 MPC 钱包 + AX ID | email (可选) |
| `get_wallet_balance` | 查询余额 | - |
| `topup_wallet` | 充值 | amount |
| `setup_agent_authorization` | 设置支付授权 | - |

### 商品购买

| 工具 | 描述 | 必需参数 |
|------|------|----------|
| `search_products` | 搜索商品 | query |
| `quick_purchase` | 一键购买 | productId |
| `prepare_checkout` | 准备结账 | productId |
| `confirm_payment` | 确认支付 | intentId |
| `collect_user_info` | 收集用户信息 | infoType |
| `check_payment_status` | 查询支付状态 | - |

### 订阅与计费

| 工具 | 描述 | 必需参数 |
|------|------|----------|
| `subscribe_service` | 订阅服务 | serviceId |
| `pay_api_usage` | 支付 API 费用 | apiId, usage |
| `check_subscription_status` | 查询订阅 | - |

### 微支付

| 工具 | 描述 | 必需参数 |
|------|------|----------|
| `x402_pay` | X402 支付 | resourceUrl |
| `x402_authorize` | X402 授权 | - |
| `pay_with_wallet` | 钱包支付 | amount |

---

## 支付方式支持

### 法币入金

| 方式 | 支持地区 | 最低金额 | 费率 |
|------|----------|----------|------|
| Stripe (信用卡/借记卡) | 全球 | ¥1 | 2.9% + ¥0.30 |
| Google Pay | 全球 | ¥1 | 2.9% + ¥0.30 |
| Apple Pay | 全球 | ¥1 | 2.9% + ¥0.30 |
| 支付宝 | 中国 | ¥1 | 1.5% |
| 微信支付 | 中国 | ¥1 | 1.5% |

### 加密货币

| 币种 | 网络 | 用途 |
|------|------|------|
| USDC | BSC / Ethereum | 主要支付币种 |
| USDT | BSC / Ethereum | 备选稳定币 |
| ETH | Ethereum | Gas 费 + 支付 |

---

## 典型用户旅程

### 新用户完整旅程

```
1. 用户在 ChatGPT 中说："帮我买个耳机"
2. Agent 搜索商品，展示结果
3. 用户选择商品
4. Agent 检测无账户，提示创建钱包
5. 用户提供邮箱，创建钱包 + AX ID
6. Agent 提示充值
7. 用户点击充值链接，用支付宝充值 100 元
8. 用户回到对话，设置 QuickPay 授权
9. Agent 执行购买，从钱包扣款
10. 支付完成，订单创建
```

### 老用户快速购买

```
1. 用户："买上次那个耳机"
2. Agent 查找历史订单
3. Agent 调用 quick_purchase
4. 系统检测 QuickPay 授权有效
5. 自动扣款，返回成功
```

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2024-01 | 初始版本，覆盖 14 种主要场景 |

---

*文档维护: Agentrix Team*
