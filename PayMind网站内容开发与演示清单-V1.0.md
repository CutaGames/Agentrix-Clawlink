# PayMind 网站内容开发与演示清单 V1.0
## 统一支付演示、Mock功能与生产环境迁移计划

**版本**: 1.0  
**日期**: 2025年1月  
**状态**: 开发中

---

## 📋 目录

1. [网站内容开发清单](#1-网站内容开发清单)
2. [统一支付演示页面](#2-统一支付演示页面)
3. [Mock功能清单](#3-mock功能清单)
4. [生产环境迁移清单](#4-生产环境迁移清单)
5. [开发优先级](#5-开发优先级)

---

## 1. 网站内容开发清单

### 1.1 核心演示页面

#### 1.1.1 统一支付演示页面（新增）

**页面路径**：`/pay/unified`

**功能描述**：
- 展示PayMind统一支付流程（7阶段）
- 支持实体商品、服务类、链上资产支付
- 支持多国家定价和税费计算
- 支持智能路由选择
- 支持QuickPay快速通道
- 支持KYC引导流程

**页面内容**：
- 支付流程步骤展示
- 价格和费用明细（商品价格、税费、佣金、通道费用）
- 支付方式选择（钱包、法币、混合支付）
- 智能路由推荐
- 支付状态跟踪
- 支付结果展示

**Mock功能**：
- ✅ 支付流程模拟
- ✅ 价格计算模拟
- ✅ 智能路由模拟
- ✅ 支付状态模拟
- ⚠️ 真实支付（待迁移）

**开发状态**：🔄 待开发

---

#### 1.1.2 佣金分配演示页面（新增）

**页面路径**：`/pay/commission-demo`

**功能描述**：
- 展示佣金分配机制
- 支持不同商品类型的佣金计算
- 支持多Agent协作场景
- 支持佣金明细展示

**页面内容**：
- 商品类型选择（实体商品、服务类、链上资产）
- 佣金计算器
- 佣金分配明细（推荐Agent、执行Agent、推广Agent、PayMind平台）
- 场景示例（单Agent、双Agent、多Agent）
- 佣金比例说明

**Mock功能**：
- ✅ 佣金计算模拟
- ✅ 佣金分配模拟
- ⚠️ 真实佣金记录（待迁移）

**开发状态**：🔄 待开发

---

#### 1.1.3 智能路由演示页面（增强）

**页面路径**：`/pay/smart-routing`

**功能描述**：
- 展示智能路由选择机制
- 支持多通道对比
- 支持QuickPay快速通道
- 支持KYC引导流程

**页面内容**：
- 用户信息输入（国家、货币、金额）
- 可用通道列表
- 通道对比（价格、费用、成功率）
- 推荐通道展示
- QuickPay授权流程
- KYC引导流程

**Mock功能**：
- ✅ 路由选择模拟
- ✅ 通道对比模拟
- ✅ QuickPay模拟
- ✅ KYC模拟
- ⚠️ 真实路由（待迁移）

**开发状态**：🔄 待增强

---

#### 1.1.4 Marketplace资产聚合演示页面（新增）

**页面路径**：`/marketplace/aggregation-demo`

**功能描述**：
- 展示资产聚合机制
- 支持平台早期聚合和用户自发资产展示
- 支持游戏资产和RWA展示
- 支持收入模式说明

**页面内容**：
- 资产类型筛选（NFT、FT、游戏资产、RWA）
- 资产来源标识（平台聚合、用户自发）
- 资产价格和佣金展示
- 收入模式说明（平台分佣、用户付费）
- 交易流程演示

**Mock功能**：
- ✅ 资产聚合模拟
- ✅ 资产展示模拟
- ✅ 交易流程模拟
- ⚠️ 真实资产聚合（待迁移）

**开发状态**：🔄 待开发

---

#### 1.1.5 多国家定价演示页面（新增）

**页面路径**：`/pay/multi-country-pricing`

**功能描述**：
- 展示多国家定价机制
- 支持税费计算
- 支持货币转换
- 支持价格对比

**页面内容**：
- 国家选择器
- 商品价格展示（基础价格、国家价格）
- 税费计算（VAT、GST、销售税）
- 货币转换
- 价格对比（不同国家）
- 税费说明

**Mock功能**：
- ✅ 价格计算模拟
- ✅ 税费计算模拟
- ✅ 货币转换模拟
- ⚠️ 真实价格数据（待迁移）

**开发状态**：🔄 待开发

---

#### 1.1.6 联盟机制演示页面（新增）

**页面路径**：`/alliance/demo`

**功能描述**：
- 展示联盟机制和佣金分配
- 支持Agent角色说明
- 支持佣金场景演示
- 支持生态激励说明

**页面内容**：
- Agent角色说明（执行Agent、推荐Agent、推广Agent）
- 佣金分配场景（单Agent、双Agent、多Agent）
- 生态激励说明（用户推荐、开发者分成）
- 佣金计算器
- 场景示例

**Mock功能**：
- ✅ 佣金计算模拟
- ✅ 场景演示模拟
- ⚠️ 真实佣金记录（待迁移）

**开发状态**：🔄 待开发

---

### 1.2 功能说明页面

#### 1.2.1 支付功能说明页面（增强）

**页面路径**：`/features/payment`

**功能描述**：
- 支付功能详细介绍
- 支付方式说明
- 支付流程说明
- 支付优势说明

**页面内容**：
- 支付方式介绍（钱包、法币、混合支付）
- 支付流程说明（7阶段）
- 支付优势（智能路由、QuickPay、KYC）
- 使用场景
- 技术特点

**开发状态**：✅ 已有，需要增强

---

#### 1.2.2 Marketplace功能说明页面（增强）

**页面路径**：`/features/marketplace`

**功能描述**：
- Marketplace功能详细介绍
- 资产类型说明
- 资产聚合说明
- 交易流程说明

**页面内容**：
- 资产类型介绍（NFT、FT、游戏资产、RWA）
- 资产聚合机制（平台聚合、用户自发）
- 交易流程说明
- 收入模式说明
- 使用场景

**开发状态**：✅ 已有，需要增强

---

#### 1.2.3 联盟机制说明页面（新增）

**页面路径**：`/features/alliance`

**功能描述**：
- 联盟机制详细介绍
- Agent角色说明
- 佣金分配说明
- 生态激励说明

**页面内容**：
- 联盟机制介绍
- Agent角色说明（执行、推荐、推广）
- 佣金分配规则
- 生态激励机制
- 使用场景

**开发状态**：🔄 待开发

---

### 1.3 文档页面

#### 1.3.1 API文档页面（增强）

**页面路径**：`/developers/api`

**功能描述**：
- API接口文档
- 支付API说明
- Marketplace API说明
- 佣金API说明

**页面内容**：
- API概览
- 支付API文档
- Marketplace API文档
- 佣金API文档
- SDK文档
- 使用示例

**开发状态**：✅ 已有，需要增强

---

#### 1.3.2 SDK文档页面（增强）

**页面路径**：`/developers/sdk`

**功能描述**：
- SDK使用文档
- SDK API说明
- SDK示例代码
- SDK迁移指南

**页面内容**：
- SDK概览
- SDK安装和配置
- SDK API文档
- SDK示例代码
- SDK迁移指南（从旧版本到新版本）

**开发状态**：✅ 已有，需要增强

---

## 2. 统一支付演示页面详细设计

### 2.1 页面结构

```
┌─────────────────────────────────────────────────────────┐
│  统一支付演示 - PayMind Unified Payment                  │
│                                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │  步骤1：选择商品类型                              │   │
│  │  [实体商品] [服务类] [链上资产]                  │   │
│  └─────────────────────────────────────────────────┘   │
│                                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │  步骤2：商品信息                                  │   │
│  │  商品名称：[输入框]                               │   │
│  │  商品价格：[输入框]                               │   │
│  │  商品国家：[选择器]                               │   │
│  └─────────────────────────────────────────────────┘   │
│                                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │  步骤3：价格和费用明细                            │   │
│  │  商品价格：$1,000                                │   │
│  │  税费（VAT 20%）：$200                            │   │
│  │  通道费用：-$0.72                                │   │
│  │  佣金：-$30                                      │   │
│  │  ─────────────────────────────────────────────   │   │
│  │  总计：$1,169.28                                 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │  步骤4：智能路由选择                              │   │
│  │  推荐通道：[Stripe] [推荐理由]                    │   │
│  │  其他通道：[列表]                                 │   │
│  └─────────────────────────────────────────────────┘   │
│                                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │  步骤5：支付方式选择                              │   │
│  │  [钱包支付] [法币支付] [混合支付]                │   │
│  └─────────────────────────────────────────────────┘   │
│                                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │  步骤6：支付执行                                  │   │
│  │  [支付进度条] [支付状态]                          │   │
│  └─────────────────────────────────────────────────┘   │
│                                                           │
│  ┌─────────────────────────────────────────────────┐   │
│  │  步骤7：支付结果                                  │   │
│  │  [支付成功/失败] [交易详情] [佣金明细]            │   │
│  └─────────────────────────────────────────────────┘   │
│                                                           │
│  [重置] [导出结果]                                        │
└─────────────────────────────────────────────────────────┘
```

### 2.2 功能实现

#### 2.2.1 Mock支付流程

```typescript
// paymindfrontend/lib/mock/unifiedPaymentMock.ts
export const unifiedPaymentMock = {
  // 步骤1：选择商品类型
  selectProductType: (type: 'physical' | 'service' | 'onchain') => {
    return {
      type,
      commissionRate: type === 'physical' ? 0.03 : type === 'service' ? 0.05 : 0.025,
      paymindFeeRate: type === 'physical' ? 0.005 : type === 'service' ? 0.01 : 0.01,
    };
  },

  // 步骤2：计算价格和费用
  calculatePrice: (product: {
    price: number;
    country: string;
    currency: string;
    productType: string;
  }) => {
    // Mock税费计算
    const taxRate = getTaxRate(product.country); // Mock函数
    const taxAmount = product.price * taxRate;
    
    // Mock通道费用
    const channelFee = product.price * 0.00072; // Stripe 2.9% + 0.3%
    
    // Mock佣金计算
    const commissionRate = product.productType === 'physical' ? 0.03 : 
                           product.productType === 'service' ? 0.05 : 0.025;
    const commission = product.price * commissionRate;
    
    return {
      basePrice: product.price,
      taxRate,
      taxAmount,
      channelFee,
      commission,
      total: product.price + taxAmount + channelFee + commission,
    };
  },

  // 步骤3：智能路由选择
  selectRoute: (payment: {
    amount: number;
    country: string;
    currency: string;
  }) => {
    // Mock路由选择
    const routes = [
      { name: 'Stripe', fee: 0.029, successRate: 0.98, recommended: true },
      { name: 'Paddle', fee: 0.03, successRate: 0.95, recommended: false },
      { name: 'Adyen', fee: 0.025, successRate: 0.97, recommended: false },
    ];
    
    return {
      recommended: routes.find(r => r.recommended),
      alternatives: routes.filter(r => !r.recommended),
    };
  },

  // 步骤4：执行支付
  executePayment: async (payment: {
    amount: number;
    method: string;
    route: string;
  }) => {
    // Mock支付执行
    await new Promise(resolve => setTimeout(resolve, 2000)); // 模拟2秒延迟
    
    return {
      success: Math.random() > 0.1, // 90%成功率
      transactionId: `tx_${Date.now()}`,
      timestamp: new Date().toISOString(),
    };
  },

  // 步骤5：获取支付结果
  getPaymentResult: (transactionId: string) => {
    return {
      transactionId,
      status: 'completed',
      amount: 1000,
      commission: {
        recommendationAgent: 9,
        executionAgent: 21,
        paymind: 5,
      },
      timestamp: new Date().toISOString(),
    };
  },
};
```

#### 2.2.2 页面组件

```typescript
// paymindfrontend/pages/pay/unified.tsx
import { useState } from 'react';
import { unifiedPaymentMock } from '@/lib/mock/unifiedPaymentMock';

export default function UnifiedPaymentDemo() {
  const [step, setStep] = useState(1);
  const [productType, setProductType] = useState<'physical' | 'service' | 'onchain'>('physical');
  const [product, setProduct] = useState({ price: 1000, country: 'US', currency: 'USD' });
  const [priceDetails, setPriceDetails] = useState(null);
  const [route, setRoute] = useState(null);
  const [paymentResult, setPaymentResult] = useState(null);

  // 步骤1：选择商品类型
  const handleProductTypeSelect = (type) => {
    setProductType(type);
    setStep(2);
  };

  // 步骤2：计算价格
  const handleCalculatePrice = () => {
    const details = unifiedPaymentMock.calculatePrice({
      ...product,
      productType,
    });
    setPriceDetails(details);
    setStep(3);
  };

  // 步骤3：选择路由
  const handleSelectRoute = () => {
    const routeResult = unifiedPaymentMock.selectRoute(product);
    setRoute(routeResult);
    setStep(4);
  };

  // 步骤4：执行支付
  const handleExecutePayment = async () => {
    const result = await unifiedPaymentMock.executePayment({
      amount: priceDetails.total,
      method: 'stripe',
      route: route.recommended.name,
    });
    setPaymentResult(result);
    setStep(5);
  };

  return (
    <div className="container mx-auto p-8">
      <h1 className="text-3xl font-bold mb-8">统一支付演示</h1>
      
      {/* 步骤指示器 */}
      <div className="flex justify-between mb-8">
        {[1, 2, 3, 4, 5].map((s) => (
          <div key={s} className={`step ${s <= step ? 'active' : ''}`}>
            步骤{s}
          </div>
        ))}
      </div>

      {/* 步骤内容 */}
      {step === 1 && <ProductTypeSelection onSelect={handleProductTypeSelect} />}
      {step === 2 && <ProductInfo product={product} onCalculate={handleCalculatePrice} />}
      {step === 3 && <PriceDetails details={priceDetails} onNext={handleSelectRoute} />}
      {step === 4 && <RouteSelection route={route} onPay={handleExecutePayment} />}
      {step === 5 && <PaymentResult result={paymentResult} />}
    </div>
  );
}
```

---

## 3. Mock功能清单

### 3.1 支付相关Mock

| 功能 | Mock状态 | 真实环境状态 | 迁移优先级 |
|------|---------|------------|-----------|
| **支付流程** | ✅ Mock | ⚠️ 待迁移 | P0 |
| **价格计算** | ✅ Mock | ⚠️ 待迁移 | P0 |
| **税费计算** | ✅ Mock | ⚠️ 待迁移 | P0 |
| **通道费用计算** | ✅ Mock | ⚠️ 待迁移 | P0 |
| **佣金计算** | ✅ Mock | ⚠️ 待迁移 | P0 |
| **智能路由选择** | ✅ Mock | ⚠️ 待迁移 | P0 |
| **QuickPay授权** | ✅ Mock | ⚠️ 待迁移 | P1 |
| **KYC检查** | ✅ Mock | ⚠️ 待迁移 | P1 |
| **支付执行** | ✅ Mock | ⚠️ 待迁移 | P0 |
| **支付状态查询** | ✅ Mock | ⚠️ 待迁移 | P0 |

### 3.2 Marketplace相关Mock

| 功能 | Mock状态 | 真实环境状态 | 迁移优先级 |
|------|---------|------------|-----------|
| **资产聚合** | ✅ Mock | ⚠️ 待迁移 | P1 |
| **资产展示** | ✅ Mock | ⚠️ 待迁移 | P1 |
| **资产价格** | ✅ Mock | ⚠️ 待迁移 | P1 |
| **资产交易** | ✅ Mock | ⚠️ 待迁移 | P1 |
| **收入模式** | ✅ Mock | ⚠️ 待迁移 | P1 |

### 3.3 联盟相关Mock

| 功能 | Mock状态 | 真实环境状态 | 迁移优先级 |
|------|---------|------------|-----------|
| **佣金分配** | ✅ Mock | ⚠️ 待迁移 | P0 |
| **Agent角色** | ✅ Mock | ⚠️ 待迁移 | P1 |
| **佣金计算** | ✅ Mock | ⚠️ 待迁移 | P0 |
| **佣金结算** | ✅ Mock | ⚠️ 待迁移 | P1 |
| **生态激励** | ✅ Mock | ⚠️ 待迁移 | P2 |

### 3.4 其他Mock

| 功能 | Mock状态 | 真实环境状态 | 迁移优先级 |
|------|---------|------------|-----------|
| **多国家定价** | ✅ Mock | ⚠️ 待迁移 | P1 |
| **货币转换** | ✅ Mock | ⚠️ 待迁移 | P1 |
| **税费计算** | ✅ Mock | ⚠️ 待迁移 | P1 |
| **Agent ID** | ✅ Mock | ⚠️ 待迁移 | P1 |
| **SLA监控** | ✅ Mock | ⚠️ 待迁移 | P2 |

---

## 4. 生产环境迁移清单

### 4.1 支付功能迁移

#### 4.1.1 支付流程迁移

**Mock实现**：`paymindfrontend/lib/mock/unifiedPaymentMock.ts`

**真实环境实现**：
- [ ] 后端API：`POST /api/payments/create`
- [ ] 后端API：`POST /api/payments/process`
- [ ] 后端API：`GET /api/payments/:id/status`
- [ ] 前端调用：`paymindfrontend/lib/api/payment.api.ts`

**迁移步骤**：
1. 实现后端支付API
2. 更新前端API调用
3. 移除Mock代码
4. 测试真实支付流程

**验收标准**：
- ✅ 支付流程完整
- ✅ 支付状态准确
- ✅ 错误处理完善

---

#### 4.1.2 价格计算迁移

**Mock实现**：`paymindfrontend/lib/mock/priceCalculationMock.ts`

**真实环境实现**：
- [ ] 后端API：`POST /api/prices/calculate`
- [ ] 后端服务：`backend/src/modules/pricing/pricing.service.ts`
- [ ] 前端调用：`paymindfrontend/lib/api/pricing.api.ts`

**迁移步骤**：
1. 实现后端价格计算服务
2. 实现税费计算服务
3. 更新前端API调用
4. 移除Mock代码
5. 测试价格计算准确性

**验收标准**：
- ✅ 价格计算准确
- ✅ 税费计算准确
- ✅ 多国家价格支持

---

#### 4.1.3 智能路由迁移

**Mock实现**：`paymindfrontend/lib/mock/smartRoutingMock.ts`

**真实环境实现**：
- [ ] 后端API：`POST /api/smart-router/route`
- [ ] 后端服务：`backend/src/modules/smart-router/smart-router.service.ts`
- [ ] 前端调用：`paymindfrontend/lib/api/smartRouter.api.ts`

**迁移步骤**：
1. 实现后端智能路由服务
2. 实现通道对比逻辑
3. 更新前端API调用
4. 移除Mock代码
5. 测试路由选择准确性

**验收标准**：
- ✅ 路由选择准确
- ✅ 通道对比准确
- ✅ QuickPay支持

---

#### 4.1.4 佣金计算迁移

**Mock实现**：`paymindfrontend/lib/mock/commissionMock.ts`

**真实环境实现**：
- [ ] 后端API：`POST /api/commissions/calculate`
- [ ] 后端服务：`backend/src/modules/commission/commission-calculator.service.ts`
- [ ] 前端调用：`paymindfrontend/lib/api/commission.api.ts`

**迁移步骤**：
1. 实现后端佣金计算服务
2. 实现佣金分配逻辑
3. 更新前端API调用
4. 移除Mock代码
5. 测试佣金计算准确性

**验收标准**：
- ✅ 佣金计算准确
- ✅ 佣金分配准确
- ✅ 多Agent协作支持

---

### 4.2 Marketplace功能迁移

#### 4.2.1 资产聚合迁移

**Mock实现**：`paymindfrontend/lib/mock/assetAggregationMock.ts`

**真实环境实现**：
- [ ] 后端API：`GET /api/marketplace/assets`
- [ ] 后端服务：`backend/src/modules/marketplace/asset-aggregation.service.ts`
- [ ] 前端调用：`paymindfrontend/lib/api/marketplace.api.ts`

**迁移步骤**：
1. 实现后端资产聚合服务
2. 实现API接口聚合
3. 实现链上数据聚合
4. 更新前端API调用
5. 移除Mock代码
6. 测试资产聚合准确性

**验收标准**：
- ✅ 资产聚合准确
- ✅ 资产信息完整
- ✅ 资产来源标识准确

---

#### 4.2.2 资产交易迁移

**Mock实现**：`paymindfrontend/lib/mock/assetTradingMock.ts`

**真实环境实现**：
- [ ] 后端API：`POST /api/marketplace/assets/:id/purchase`
- [ ] 后端服务：`backend/src/modules/marketplace/asset-trading.service.ts`
- [ ] 前端调用：`paymindfrontend/lib/api/marketplace.api.ts`

**迁移步骤**：
1. 实现后端资产交易服务
2. 实现平台分佣模式
3. 实现用户付费模式
4. 更新前端API调用
5. 移除Mock代码
6. 测试资产交易流程

**验收标准**：
- ✅ 资产交易流程完整
- ✅ 收入模式正确
- ✅ 交易状态准确

---

### 4.3 联盟功能迁移

#### 4.3.1 佣金分配迁移

**Mock实现**：`paymindfrontend/lib/mock/commissionAllocationMock.ts`

**真实环境实现**：
- [ ] 后端API：`POST /api/commissions/allocate`
- [ ] 后端服务：`backend/src/modules/commission/commission-allocator.service.ts`
- [ ] 前端调用：`paymindfrontend/lib/api/commission.api.ts`

**迁移步骤**：
1. 实现后端佣金分配服务
2. 实现Agent角色识别
3. 实现佣金分配逻辑
4. 更新前端API调用
5. 移除Mock代码
6. 测试佣金分配准确性

**验收标准**：
- ✅ 佣金分配准确
- ✅ Agent角色识别准确
- ✅ 多Agent协作支持

---

### 4.4 迁移时间表

| 功能模块 | Mock完成时间 | 真实环境开发 | 测试时间 | 上线时间 |
|---------|------------|------------|---------|---------|
| **支付流程** | Week 1 | Week 2-3 | Week 4 | Week 5 |
| **价格计算** | Week 1 | Week 2-3 | Week 4 | Week 5 |
| **智能路由** | Week 1 | Week 3-4 | Week 5 | Week 6 |
| **佣金计算** | Week 1 | Week 2-3 | Week 4 | Week 5 |
| **资产聚合** | Week 2 | Week 4-5 | Week 6 | Week 7 |
| **资产交易** | Week 2 | Week 5-6 | Week 7 | Week 8 |
| **佣金分配** | Week 2 | Week 3-4 | Week 5 | Week 6 |

---

## 5. 开发优先级

### 5.1 P0优先级（必须）

1. **统一支付演示页面** - 核心演示功能
2. **支付流程Mock** - 基础功能
3. **价格计算Mock** - 基础功能
4. **佣金计算Mock** - 基础功能
5. **支付流程迁移** - 核心功能
6. **价格计算迁移** - 核心功能
7. **佣金计算迁移** - 核心功能

### 5.2 P1优先级（重要）

1. **智能路由演示页面** - 重要演示功能
2. **Marketplace资产聚合演示** - 重要演示功能
3. **联盟机制演示页面** - 重要演示功能
4. **智能路由迁移** - 重要功能
5. **资产聚合迁移** - 重要功能
6. **佣金分配迁移** - 重要功能

### 5.3 P2优先级（可选）

1. **多国家定价演示页面** - 可选演示功能
2. **API文档增强** - 可选功能
3. **SDK文档增强** - 可选功能
4. **SLA监控迁移** - 可选功能

---

## 6. 总结

### 6.1 开发任务汇总

**新增页面**：6个
- 统一支付演示页面
- 佣金分配演示页面
- Marketplace资产聚合演示页面
- 多国家定价演示页面
- 联盟机制演示页面
- 联盟机制说明页面

**增强页面**：3个
- 智能路由演示页面
- 支付功能说明页面
- Marketplace功能说明页面

**Mock功能**：15个
- 支付相关：10个
- Marketplace相关：5个
- 联盟相关：5个
- 其他：5个

**迁移任务**：10个
- 支付功能：4个
- Marketplace功能：2个
- 联盟功能：1个
- 其他：3个

### 6.2 开发时间估算

**Mock开发**：1-2周
**真实环境开发**：4-6周
**测试和优化**：2-3周
**总计**：7-11周

---

**请确认开发优先级和时间安排，以便开始开发工作。**

