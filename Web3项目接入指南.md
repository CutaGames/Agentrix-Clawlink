# Agentrix Web3 项目接入指南
## 快速接入，批量导入商品

**版本**: 3.0  
**日期**: 2025年1月

---

## 🎯 接入价值

### 为什么接入 Agentrix？

1. **获得 AI Agent 流量**
   - 所有接入 Agentrix 的 Agent 都可以推荐您的商品
   - AI 驱动的自动交易带来新订单

2. **批量导入商品**
   - 一键导入平台内所有商品
   - 自动同步价格和库存
   - 无需逐个添加

3. **获得收益分成**
   - NFT 交易平台：0.3% - 0.5%
   - DEX：0.2% - 0.4%
   - 链上游戏：0.3% - 0.5%
   - 平台接入奖励：$2,000 - $20,000

4. **技术对接简单**
   - 提供完整的 API 和 SDK
   - 支持 API 接入和链上同步
   - 专属技术支持

---

## 📋 接入流程

### Step 1: 申请接入（5分钟）

1. **填写接入申请表**
   - 访问：https://agentrix.ai/partners/apply
   - 填写项目基本信息
   - 选择项目类型（NFT 市场/DEX/游戏/服务）

2. **审核通过**
   - Agentrix 团队审核（1-2 个工作日）
   - 审核通过后创建项目账户
   - 获得 API 密钥和 Dashboard 访问权限

### Step 2: 选择接入方式（10分钟）

#### 方式1：API 接入（推荐）

**适用场景**：项目方有完整的 API 接口

**优势**：
- 数据同步准确
- 支持实时同步
- 灵活的数据映射

**要求**：
- 提供商品列表 API
- 提供商品详情 API
- 提供价格查询 API

#### 方式2：链上同步

**适用场景**：项目方没有 API，但资产在链上

**优势**：
- 无需开发 API
- 自动监听链上事件
- 实时同步链上数据

**要求**：
- 提供合约地址
- 提供 Token 标准（ERC-721/ERC-1155/SPL）
- 提供元数据 URI 格式

#### 方式3：混合方式

**适用场景**：部分商品有 API，部分在链上

**优势**：
- 灵活组合
- 覆盖所有商品

### Step 3: 技术对接（1-2 天）

#### API 接入对接

```typescript
// 1. 安装 SDK
npm install @agentrix/project-sdk

// 2. 初始化
import { AgentrixProject } from '@agentrix/project-sdk'

const project = new AgentrixProject({
  projectId: 'your-project-id',
  apiKey: 'your-api-key',
  apiSecret: 'your-api-secret'
})

// 3. 批量导入商品
const products = await yourAPI.getProducts()

await project.products.batchImport({
  source: 'api',
  products: products.map(p => ({
    externalId: p.id,
    name: p.name,
    assetType: 'nft',
    chain: 'ethereum',
    contractAddress: p.contract,
    tokenId: p.tokenId,
    tokenStandard: 'ERC-721',
    price: p.price,
    currency: 'USDC',
    metadata: {
      collection: p.collection,
      attributes: p.attributes
    }
  }))
})
```

#### 链上同步对接

```typescript
// 1. 配置链上同步
await project.sync.configureOnchain({
  chain: 'ethereum',
  contractAddress: '0x...',
  tokenStandard: 'ERC-721',
  syncEvents: [
    { event: 'Transfer', handler: 'handleTransfer' }
  ]
})

// 2. 启动同步
await project.sync.start()
```

### Step 4: 测试验证（1天）

1. **沙箱环境测试**
   - 在测试环境导入商品
   - 验证数据准确性
   - 测试同步功能

2. **生产环境上线**
   - 灰度发布（10% → 50% → 100%）
   - 监控同步状态
   - 处理问题

### Step 5: 持续运营

1. **监控同步状态**
   - 查看 Dashboard 同步状态
   - 检查商品数据准确性
   - 处理同步错误

2. **优化同步规则**
   - 调整同步频率
   - 优化数据映射
   - 提升同步效率

3. **查看收益**
   - 实时查看交易分成
   - 申请提现
   - 查看收益报表

---

## 💰 收益分成

### NFT 交易平台

**分成比例**：0.3% - 0.5%（根据平台规模）

**收益计算**：
```
月 GMV：$5,000,000
分成比例：0.4%
月收益 = $5,000,000 × 0.4% = $20,000
```

**额外奖励**：
- 平台接入奖励：$5,000 - $20,000
- GMV 里程碑奖励：
  - $500K/月：$2,500
  - $5M/月：$25,000
  - $50M/月：$250,000

### DEX

**分成比例**：0.2% - 0.4%（根据 DEX 规模）

**收益计算**：
```
月 GMV：$20,000,000
分成比例：0.3%
月收益 = $20,000,000 × 0.3% = $60,000
```

**额外奖励**：
- 平台接入奖励：$5,000 - $20,000
- 流动性提供奖励：流动性提供者奖励的 10%

### 链上游戏

**分成比例**：0.3% - 0.5%（根据游戏规模）

**收益计算**：
```
月 GMV：$1,000,000
分成比例：0.4%
月收益 = $1,000,000 × 0.4% = $4,000
```

---

## 🔧 技术文档

### API 文档

- **项目方管理 API**：https://docs.agentrix.ai/api/projects
- **商品导入 API**：https://docs.agentrix.ai/api/products
- **链上同步 API**：https://docs.agentrix.ai/api/sync

### SDK 文档

- **JavaScript/TypeScript SDK**：https://docs.agentrix.ai/sdk/js
- **Python SDK**：https://docs.agentrix.ai/sdk/python

### 示例代码

- **NFT 市场接入示例**：https://github.com/agentrix/examples/nft-marketplace
- **DEX 接入示例**：https://github.com/agentrix/examples/dex
- **链上游戏接入示例**：https://github.com/agentrix/examples/game

---

## 📞 联系方式

**技术支持**：
- 邮箱：support@agentrix.ai
- Discord：https://discord.gg/agentrix
- GitHub：https://github.com/agentrix

**商务合作**：
- 邮箱：partners@agentrix.ai
- 官网：https://agentrix.ai/partners

---

*快速接入，立即获得 AI Agent 流量！*

