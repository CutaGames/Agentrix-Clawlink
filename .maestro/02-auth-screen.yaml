appId: app.clawlink.mobile
---
# Test 02: Auth screen shows correct branding and all login tabs

- launchApp:
    clearState: true

- waitForAnimationToEnd:
    timeout: 8000

# Logo visible
- assertVisible: "🦀 ClawLink"
- takeScreenshot: "02-auth-branding"

# Default mode is OpenClaw — check OpenClaw input placeholder is visible
- assertVisible:
    textRegex: "Connect & Sign In|OpenClaw|instance"

# Switch to Social tab
- tapOn:
    textRegex: "Social|social account"

- waitForAnimationToEnd:
    timeout: 2000

- assertVisible:
    textRegex: "Google|Sign in with social"

- takeScreenshot: "02-social-tab"

# Switch to Email tab
- tapOn:
    textRegex: "Email|email"

- waitForAnimationToEnd:
    timeout: 2000

- assertVisible:
    textRegex: "Email|password|Password"

- takeScreenshot: "02-email-tab"
