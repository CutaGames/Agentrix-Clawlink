# 启动后台管理服务指南

## 🚀 快速启动

### 方式1: 使用ts-node（推荐，无需额外安装）

```bash
# 在backend目录下
npm run start:admin
```

### 方式2: 开发模式（支持热重载）

如果需要热重载功能，先安装ts-node-dev：

```bash
cd backend
npm install --save-dev ts-node-dev
```

然后运行：

```bash
npm run start:admin:dev
```

### 方式3: 使用nest CLI（如果已安装）

```bash
cd backend
nest start --watch --entryFile main-admin
```

## ⚠️ 常见问题

### 问题1: ts-node-dev未找到

**解决方案1**: 安装ts-node-dev
```bash
cd backend
npm install --save-dev ts-node-dev
```

**解决方案2**: 使用ts-node（已包含在依赖中）
```bash
npm run start:admin
```

### 问题2: 端口被占用

如果3002端口被占用，可以修改环境变量：

```bash
ADMIN_PORT=3003 npm run start:admin
```

或在`.env`文件中设置：
```
ADMIN_PORT=3002
```

### 问题3: 模块未找到

确保已安装所有依赖：

```bash
cd backend
npm install
```

## 📝 启动后验证

启动成功后，应该看到：

```
🚀 PayMind Admin API is running on: http://0.0.0.0:3002
📚 Admin API Documentation: http://0.0.0.0:3002/api/docs
```

然后可以访问：
- API文档: http://localhost:3002/api/docs
- 健康检查: http://localhost:3002/api/health

