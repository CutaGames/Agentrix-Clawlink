# V7.0 统一支付流程验证清单

**创建日期**: 2025-01-24  
**目标**: 按照V7.0时序图逐步验证统一支付流程是否通畅

---

## ✅ 完成状态确认

根据代码检查，V7.0统一支付**代码已全部实现**：

- ✅ ERC-8004合约 (`contract/contracts/ERC8004SessionManager.sol`)
- ✅ Relayer服务 (`backend/src/modules/relayer/`)
- ✅ Pre-Flight Check (`backend/src/modules/payment/preflight-check.service.ts`)
- ✅ Session管理 (`backend/src/modules/session/`)
- ✅ 前端组件 (`paymindfrontend/components/payment/SmartCheckout.tsx`)
- ✅ 模块已注册到 `app.module.ts`

**但流程可能不通的原因**：
1. 环境变量未配置（合约地址、Relayer私钥等）
2. 合约未部署
3. 数据库迁移未执行
4. API路由未正确注册
5. 前端调用路径错误

---

## 🔍 第一步：环境检查

### 1.1 检查后端服务启动

```bash
cd backend
npm run start:dev
```

**验证点**：
- [ ] 服务启动成功（无端口冲突）
- [ ] 数据库连接成功
- [ ] 查看日志中是否有以下信息：
  ```
  [Nest] INFO  PayMindRelayerService Relayer initialized
  [Nest] INFO  PreflightCheckService Pre-flight check service initialized
  [Nest] INFO  SessionService Session service initialized
  ```

**如果失败**：
- 检查端口3001是否被占用（之前已发现）
- 检查数据库连接配置
- 检查环境变量 `.env` 文件

### 1.2 检查环境变量配置

检查 `backend/.env` 文件，确保以下变量已配置：

```env
# Relayer配置（必需）
RELAYER_PRIVATE_KEY=your_relayer_private_key
RPC_URL=https://your-rpc-url
ERC8004_CONTRACT_ADDRESS=0x...  # 合约地址（如果已部署）
USDC_ADDRESS=0x...  # USDC代币地址

# Provider配置（可选，用于Crypto-Rail）
MOONPAY_API_KEY=your_moonpay_api_key
PAYMIND_CONTRACT_ADDRESS=0x...
```

**验证点**：
- [ ] `RELAYER_PRIVATE_KEY` 已配置
- [ ] `RPC_URL` 已配置（测试网或主网）
- [ ] `ERC8004_CONTRACT_ADDRESS` 已配置（如果合约已部署）

**如果未配置**：
- 合约地址：需要先部署合约（见第二步）
- Relayer私钥：生成一个新的EOA钱包私钥
- RPC URL：使用Infura、Alchemy等RPC服务

---

## 📦 第二步：合约部署检查

### 2.1 检查合约是否已部署

```bash
cd contract
npx hardhat run scripts/deploy-erc8004.ts --network <network>
```

**验证点**：
- [ ] 合约部署成功
- [ ] 获得合约地址
- [ ] 合约地址已写入 `.env` 文件

**如果未部署**：
1. 配置 `hardhat.config.ts` 网络设置
2. 准备部署账户（有足够ETH支付Gas）
3. 执行部署脚本
4. 将合约地址写入 `.env`

### 2.2 设置Relayer地址

部署合约后，需要设置Relayer地址：

```typescript
// 在合约中调用
await sessionManager.setRelayer(relayerAddress);
```

**验证点**：
- [ ] Relayer地址已设置到合约
- [ ] Relayer钱包有足够ETH支付Gas

---

## 🗄️ 第三步：数据库迁移检查

### 3.1 检查数据库迁移

```bash
cd backend
npm run migration:run
```

**验证点**：
- [ ] 迁移执行成功
- [ ] `agent_sessions` 表已创建
- [ ] 表结构正确（包含 `sessionId`, `userId`, `status` 等字段）

**如果失败**：
- 检查数据库连接
- 检查迁移文件是否存在
- 查看错误日志

### 3.2 验证表结构

```sql
-- 在PostgreSQL中执行
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'agent_sessions';
```

**预期字段**：
- `id` (uuid)
- `sessionId` (varchar)
- `userId` (uuid)
- `signer` (varchar)
- `singleLimit` (numeric)
- `dailyLimit` (numeric)
- `expiry` (timestamp)
- `status` (enum)
- `createdAt` (timestamp)
- `updatedAt` (timestamp)

---

## 🔌 第四步：API端点验证

### 4.1 测试Pre-Flight Check API

```bash
# 先登录获取Token
TOKEN=$(curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password"}' \
  | jq -r '.access_token')

# 测试Pre-Flight Check
curl -X GET "http://localhost:3001/api/payment/preflight?amount=10&currency=USDC" \
  -H "Authorization: Bearer $TOKEN"
```

**预期响应**：
```json
{
  "recommendedRoute": "quickpay" | "wallet" | "crypto-rail",
  "quickPayAvailable": true | false,
  "sessionLimit": { ... },
  "walletBalance": "1000.00",
  "requiresKYC": false,
  "estimatedTime": "< 1 second"
}
```

**验证点**：
- [ ] API返回200状态码
- [ ] 返回正确的路由建议
- [ ] 如果失败，查看错误信息

**常见错误**：
- `401 Unauthorized` → Token无效或过期
- `500 Internal Server Error` → 检查后端日志
- `RPC连接失败` → 检查RPC_URL配置

### 4.2 测试Session创建API

```bash
curl -X POST "http://localhost:3001/api/sessions" \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "signer": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb",
    "singleLimit": 10000000,
    "dailyLimit": 100000000,
    "expiryDays": 30
  }'
```

**验证点**：
- [ ] Session创建成功
- [ ] 返回 `sessionId`
- [ ] 数据库中有记录

**如果失败**：
- 检查合约地址配置
- 检查RPC连接
- 检查用户钱包余额（需要USDC授权）

### 4.3 测试Relayer QuickPay API

```bash
curl -X POST "http://localhost:3001/api/relayer/quickpay" \
  -H "Content-Type: application/json" \
  -d '{
    "sessionId": "0x...",
    "paymentId": "payment_123",
    "to": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb",
    "amount": "1000000",
    "signature": "0x...",
    "nonce": 1234567890
  }'
```

**验证点**：
- [ ] QuickPay处理成功
- [ ] 返回即时确认
- [ ] 支付状态更新为 `COMPLETED`

**如果失败**：
- 检查签名是否正确
- 检查Session状态（是否激活、是否过期）
- 检查额度（单笔限额、每日限额）
- 检查Relayer钱包Gas余额

---

## 🖥️ 第五步：前端功能验证

### 5.1 检查前端服务

```bash
cd paymindfrontend
npm run dev
```

**验证点**：
- [ ] 前端启动成功（http://localhost:3000）
- [ ] 无编译错误
- [ ] 可以访问支付页面

### 5.2 测试Session Key生成

在浏览器控制台执行：

```javascript
// 检查SessionKeyManager是否可用
import { SessionKeyManager } from '@/lib/session-key-manager';

// 生成Session Key
const sessionKey = await SessionKeyManager.generateSessionKey();
console.log('Session Key:', sessionKey);
```

**验证点**：
- [ ] Session Key生成成功
- [ ] 私钥已加密存储到IndexedDB
- [ ] 可以列出所有Session Key

### 5.3 测试智能收银台

访问支付页面，触发支付流程：

**验证点**：
- [ ] Pre-Flight Check自动执行（查看Network请求）
- [ ] UI根据路由结果动态渲染
- [ ] QuickPay按钮显示（如果有Session）
- [ ] 支付流程正常执行

**如果失败**：
- 打开浏览器开发者工具（F12）
- 查看Console错误信息
- 查看Network请求失败原因
- 检查API调用路径是否正确

### 5.4 测试Session管理界面

访问Session管理页面：

**验证点**：
- [ ] Session列表显示正常
- [ ] 创建Session表单可用
- [ ] 撤销Session功能正常
- [ ] 限额显示正确

---

## 🔄 第六步：完整流程验证

### 6.1 QuickPay完整流程

按照V7.0时序图验证：

1. **用户创建Session**
   - [ ] 前端生成Session Key
   - [ ] 调用 `/api/sessions` 创建Session
   - [ ] 合约中Session已创建（链上验证）

2. **Pre-Flight Check**
   - [ ] 调用 `/api/payment/preflight`
   - [ ] 返回 `quickPayAvailable: true`
   - [ ] UI显示QuickPay按钮

3. **QuickPay支付**
   - [ ] 用户点击支付
   - [ ] 前端使用Session Key签名
   - [ ] 调用 `/api/relayer/quickpay`
   - [ ] Relayer验证签名（链下）
   - [ ] 即时返回成功（< 1秒）
   - [ ] 支付状态更新为 `COMPLETED`

4. **异步上链**
   - [ ] Relayer批量上链（查看日志）
   - [ ] 链上交易成功（查看区块浏览器）
   - [ ] 支付记录更新

**验证点**：
- [ ] 整个流程在1秒内完成（除上链）
- [ ] 支付状态正确更新
- [ ] 链上交易成功

### 6.2 Wallet支付流程

1. **Pre-Flight Check**
   - [ ] 返回 `recommendedRoute: "wallet"`
   - [ ] UI显示钱包支付按钮

2. **钱包支付**
   - [ ] 用户连接钱包
   - [ ] 用户签名交易
   - [ ] 链上确认
   - [ ] 支付状态更新

### 6.3 Crypto-Rail流程

1. **Pre-Flight Check**
   - [ ] 返回 `recommendedRoute: "crypto-rail"`
   - [ ] 显示Provider选择

2. **Provider支付**
   - [ ] 跳转到Provider页面
   - [ ] 完成支付
   - [ ] 回调处理
   - [ ] 支付状态更新

---

## 🐛 常见问题排查

### 问题1：Relayer初始化失败

**症状**：后端启动时Relayer服务初始化失败

**排查**：
1. 检查 `RELAYER_PRIVATE_KEY` 是否配置
2. 检查 `RPC_URL` 是否可访问
3. 检查 `ERC8004_CONTRACT_ADDRESS` 是否正确
4. 查看后端日志错误信息

**解决**：
- 配置正确的环境变量
- 确保RPC服务可用
- 如果合约未部署，先部署合约

### 问题2：Pre-Flight Check返回错误

**症状**：API返回500错误或超时

**排查**：
1. 检查RPC连接（链上查询可能失败）
2. 检查用户钱包连接状态
3. 检查Session查询逻辑
4. 查看后端日志

**解决**：
- 使用可靠的RPC服务（Infura、Alchemy）
- 添加缓存机制（减少链上查询）
- 优化查询逻辑

### 问题3：QuickPay签名验证失败

**症状**：Relayer返回 `Invalid signature`

**排查**：
1. 检查签名消息格式（与合约一致）
2. 检查Session Key是否正确
3. 检查Chain ID是否匹配
4. 检查nonce是否递增

**解决**：
- 确保前端签名逻辑与合约一致
- 使用EIP-712标准签名
- 确保nonce管理正确

### 问题4：前端组件不显示

**症状**：SmartCheckout组件不渲染或报错

**排查**：
1. 检查组件导入路径
2. 检查依赖是否安装
3. 查看浏览器Console错误
4. 检查API调用是否成功

**解决**：
- 确保所有依赖已安装
- 检查TypeScript类型错误
- 确保API端点正确

---

## 📊 验证结果记录

完成以上验证后，记录结果：

### 环境配置
- [ ] 后端服务启动：✅ / ❌
- [ ] 环境变量配置：✅ / ❌
- [ ] 合约部署：✅ / ❌
- [ ] 数据库迁移：✅ / ❌

### API端点
- [ ] Pre-Flight Check：✅ / ❌
- [ ] Session创建：✅ / ❌
- [ ] Relayer QuickPay：✅ / ❌

### 前端功能
- [ ] Session Key生成：✅ / ❌
- [ ] 智能收银台：✅ / ❌
- [ ] Session管理：✅ / ❌

### 完整流程
- [ ] QuickPay流程：✅ / ❌
- [ ] Wallet支付：✅ / ❌
- [ ] Crypto-Rail：✅ / ❌

---

## 🎯 下一步行动

根据验证结果：

1. **如果所有验证通过**：
   - 流程已通畅，可以正常使用
   - 建议进行压力测试
   - 建议添加监控和告警

2. **如果有验证失败**：
   - 根据上述排查步骤定位问题
   - 修复配置或代码问题
   - 重新验证

3. **如果部分功能失败**：
   - 优先修复核心功能（QuickPay）
   - 其他功能可以后续优化

---

**最后更新**: 2025-01-24  
**维护者**: PayMind 开发团队

