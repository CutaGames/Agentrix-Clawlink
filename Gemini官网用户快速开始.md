# Gemini 官网用户快速开始

## 🎯 一句话说明

**让 Gemini 官网用户可以在对话框中直接搜索和交易 Agentrix Marketplace 的商品。**

## ⚡ 快速开始（3 步）

### Step 1: 部署 API 到公网（需要 HTTPS）

```bash
# 使用 ngrok（开发测试）
ngrok http 3001

# 或部署到服务器（生产环境）
# 确保使用 HTTPS
```

### Step 2: 用户调用 API

用户只需要：

```bash
curl -X POST https://your-api.com/api/gemini/chat \
  -H "Content-Type: application/json" \
  -d '{
    "messages": [
      {"role": "user", "content": "我要买 iPhone 15"}
    ],
    "geminiApiKey": "用户自己的-Gemini-API-Key",
    "context": {
      "sessionId": "user-session-123"
    }
  }'
```

### Step 3: 完成！

系统会自动：
1. ✅ 使用用户的 Gemini API Key 调用 Gemini
2. ✅ 自动配置 Function Calling（包含所有 Agentrix Functions）
3. ✅ 处理 Function Calls（搜索商品、加入购物车、创建订单等）
4. ✅ 返回结果

## 📝 可用的 Functions

用户可以在对话中使用以下功能：

- **搜索商品**："我要买 iPhone 15"
- **加入购物车**："把第一个商品加入购物车"
- **查看购物车**："查看我的购物车"
- **结算购物车**："结算购物车"
- **购买商品**："直接购买第一个商品"
- **查询订单**："查看我的订单"
- **支付订单**："支付订单 12345"

## 🔗 API 端点

- **对话接口**：`POST /api/gemini/chat`
- **Function Schemas**：`GET /api/gemini/functions`
- **执行 Function**：`POST /api/gemini/function-call`
- **OpenAPI 文档**：`GET /api/gemini/openapi.json`

## 📚 详细文档

- `Gemini官网用户使用指南.md` - 完整使用说明
- `Gemini-3集成配置指南.md` - 技术配置说明

