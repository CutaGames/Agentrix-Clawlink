# 📤 手动上传到 GitHub 命令指南

## 仓库信息
- **GitHub 仓库**: https://github.com/CutaGames/Agentrix

---

## 🎯 快速命令（复制粘贴执行）

### 方案 A：一键执行（推荐）

```bash
cd "d:/wsl/Ubuntu-24.04/Code/Paymind/agentrix-website"

# 1. 检查并清理 Git 缓存（确保 .gitignore 生效）
git rm -r --cached .
git add .

# 2. 关联远程仓库
git remote remove origin 2>nul
git remote add origin https://github.com/CutaGames/Agentrix.git

# 3. 拉取远程代码（如果仓库不是空的）
git pull origin main --allow-unrelated-histories

# 4. 提交代码
git commit -m "Update: Agentrix project - exclude .md, .log, .env files"

# 5. 推送到 GitHub
git push -u origin main
```

---

## 📋 分步执行（详细版）

### 步骤 1: 进入项目目录

```bash
cd "d:/wsl/Ubuntu-24.04/Code/Paymind/agentrix-website"
```

### 步骤 2: 检查 .gitignore 是否生效

```bash
# 查看当前状态
git status

# 查看被忽略的文件
git status --ignored
```

**应该看到**:
- ✅ `.env` 文件被忽略
- ✅ `*.log` 文件被忽略  
- ✅ `*.md` 文件被忽略

### 步骤 3: 清理 Git 缓存（重要！）

```bash
# 清除所有文件的 Git 缓存
git rm -r --cached .

# 重新添加文件（这次会应用 .gitignore）
git add .
```

### 步骤 4: 关联远程仓库

```bash
# 查看现有远程仓库
git remote -v

# 如果已经有 origin，先删除
git remote remove origin

# 添加你的 GitHub 仓库
git remote add origin https://github.com/CutaGames/Agentrix.git

# 验证
git remote -v
```

### 步骤 5: 拉取远程代码（合并历史）

```bash
# 如果你的 GitHub 仓库不是空的，需要先拉取
git pull origin main --allow-unrelated-histories

# 如果有冲突，解决后执行
git add .
git commit -m "Merge remote changes"
```

### 步骤 6: 查看将要上传的文件

```bash
# 查看暂存区的文件
git status

# 确认以下文件 NOT 在列表中：
# ❌ .env, backend/.env, agentrixfrontend/.env.local
# ❌ *.log, backend.log, frontend.log
# ❌ *.md 文件（如果你不想上传）
```

### 步骤 7: 提交到本地仓库

```bash
git commit -m "Update: Agentrix project - exclude sensitive files"
```

### 步骤 8: 推送到 GitHub

```bash
# 首次推送
git push -u origin main

# 如果提示需要登录，输入：
# 用户名: CutaGames
# 密码: 你的 GitHub Personal Access Token（不是密码！）
```

---

## 🔑 如何获取 GitHub Token（如果需要）

1. 访问: https://github.com/settings/tokens
2. 点击 **"Generate new token"** → **"Generate new token (classic)"**
3. 勾选权限:
   - ✅ `repo` (完整仓库访问)
4. 点击 **"Generate token"**
5. **复制 token**（只显示一次，保存好！）
6. 使用 token 作为密码推送:

```bash
# 格式
git push https://你的用户名:你的token@github.com/CutaGames/Agentrix.git main

# 或配置 credential helper
git config --global credential.helper store
git push -u origin main
# 第一次输入用户名和 token，之后自动记住
```

---

## ⚠️ 常见问题

### 问题 1: .env 文件还是被上传了

**原因**: Git 缓存中已经有这些文件

**解决**:
```bash
# 从 Git 中删除但保留本地文件
git rm --cached backend/.env
git rm --cached agentrixfrontend/.env.local
git rm --cached contract/.env

# 提交删除
git commit -m "Remove .env files from Git"
git push
```

### 问题 2: .md 文件不想全部排除

**修改 `.gitignore`**:
```gitignore
# 不要用 *.md，而是指定排除的文件
部署到腾讯云完整教程.md
快速开始-三步部署.md
BSC测试网*.md
Agentrix-*.md

# 保留 README.md
!README.md
```

### 问题 3: 推送失败 "rejected"

**原因**: 远程有新代码

**解决**:
```bash
# 先拉取
git pull origin main --rebase

# 再推送
git push -u origin main
```

### 问题 4: 提示 "unrelated histories"

**解决**:
```bash
git pull origin main --allow-unrelated-histories
```

---

## ✅ 验证上传成功

1. 访问: https://github.com/CutaGames/Agentrix
2. 检查:
   - ✅ 代码文件已上传
   - ❌ `.env` 文件不在仓库中
   - ❌ `*.log` 文件不在仓库中
   - ❌ `*.md` 文件不在仓库中（如果你选择排除）

---

## 🔄 后续更新代码

以后更新代码，只需要：

```bash
cd "d:/wsl/Ubuntu-24.04/Code/Paymind/agentrix-website"
git add .
git commit -m "更新说明"
git push
```

或直接运行: **`上传到GitHub指定仓库.bat`**

---

## 📞 需要帮助？

- 查看 Git 状态: `git status`
- 查看提交历史: `git log --oneline`
- 撤销上次提交: `git reset --soft HEAD~1`
- 强制推送（慎用）: `git push -f origin main`
