# 🚀 Agentrix 三步快速部署指南

> **目标**: 5分钟内完成 GitHub 上传 + 腾讯云部署

---

## 📦 第一步：上传到 GitHub（2分钟）

### 方法 A：使用脚本（推荐）

1. **双击运行** `快速上传到GitHub.bat`
2. **首次使用需要配置远程仓库**：

```bash
# 在 GitHub 创建仓库后，执行：
git remote add origin https://github.com/你的用户名/agentrix-website.git
```

3. **重新运行脚本** 完成上传

### 方法 B：手动执行

```bash
# 1. 添加文件
git add .

# 2. 提交
git commit -m "Initial commit"

# 3. 关联远程仓库
git remote add origin https://github.com/你的用户名/agentrix-website.git

# 4. 推送
git push -u origin main
```

### ✅ 验证成功
- 打开 `https://github.com/你的用户名/agentrix-website`
- 看到所有文件已上传

---

## ☁️ 第二步：准备腾讯云服务器（1分钟）

### 1. 购买服务器

访问: https://console.cloud.tencent.com/lighthouse

| 配置项 | 推荐值 |
|--------|--------|
| 地域 | 广州/上海（离你最近） |
| 镜像 | Ubuntu 22.04 LTS |
| 套餐 | 2核4G（最低配置） |
| 时长 | 按需选择 |

### 2. 配置防火墙

在服务器详情页 → 防火墙 → 添加规则：

```
HTTP(80)      - 允许
HTTPS(443)    - 允许
TCP:3000      - 允许（前端）
TCP:3001      - 允许（后端）
```

### 3. 获取服务器信息

- **公网 IP**: `123.456.789.0`（示例）
- **密码**: 购买时设置的 root 密码

---

## 🎯 第三步：一键部署（2分钟）

### 1. 登录服务器

```bash
# Windows PowerShell
ssh root@你的服务器IP
# 输入密码
```

### 2. 上传部署脚本

```bash
# 在服务器上执行
wget https://raw.githubusercontent.com/你的用户名/agentrix-website/main/服务器部署脚本.sh

# 或手动创建
nano deploy.sh
# 复制 服务器部署脚本.sh 的内容
```

### 3. 运行部署脚本

```bash
chmod +x deploy.sh
sudo ./deploy.sh
```

### 4. 配置环境变量

**后端配置** (`/var/www/agentrix-website/backend/.env`):

```bash
nano /var/www/agentrix-website/backend/.env
```

```env
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=agentrix
DB_PASSWORD=your_secure_password_change_this
DB_DATABASE=agentrix

JWT_SECRET=your_super_secret_jwt_key_change_this
JWT_EXPIRES_IN=7d

PORT=3001
NODE_ENV=production
CORS_ORIGIN=http://你的服务器IP:3000
FRONTEND_URL=http://你的服务器IP:3000

# 复制你本地 backend/.env 的其他配置...
```

**前端配置** (`/var/www/agentrix-website/agentrixfrontend/.env.local`):

```bash
nano /var/www/agentrix-website/agentrixfrontend/.env.local
```

```env
NEXT_PUBLIC_API_URL=http://你的服务器IP:3001/api
NEXT_PUBLIC_FRONTEND_URL=http://你的服务器IP:3000
```

### 5. 安装依赖并启动

```bash
# 进入项目目录
cd /var/www/agentrix-website

# 安装并启动后端
cd backend
npm install
npm run build
npm run migration:run

# 安装并构建前端
cd ../agentrixfrontend
npm install
npm run build

# 使用 PM2 启动
cd ..
pm2 start ecosystem.config.js
pm2 save
pm2 startup
```

---

## ✅ 验证部署成功

### 1. 检查服务状态

```bash
pm2 status
```

应该看到：
```
┌────┬───────────────────┬─────────┬─────────┬──────────┐
│ id │ name              │ status  │ restart │ uptime   │
├────┼───────────────────┼─────────┼─────────┼──────────┤
│ 0  │ agentrix-backend   │ online  │ 0       │ 10s      │
│ 1  │ agentrix-frontend  │ online  │ 0       │ 10s      │
└────┴───────────────────┴─────────┴─────────┴──────────┘
```

### 2. 访问测试

- **前端**: `http://你的服务器IP:3000`
- **后端 API**: `http://你的服务器IP:3001/api`
- **API 文档**: `http://你的服务器IP:3001/api-docs`

---

## 🎉 完成！

你的 Agentrix 项目已经成功部署到腾讯云！

### 📊 后续优化（可选）

1. **配置域名**: 见 `部署到腾讯云完整教程.md` 第 5 章
2. **启用 HTTPS**: 使用 Let's Encrypt 免费证书
3. **配置 Nginx**: 反向代理 + 负载均衡
4. **监控告警**: 配置 PM2 监控

---

## 🆘 常见问题快速修复

### 问题 1: 服务无法访问
```bash
# 检查防火墙
sudo ufw status
sudo ufw allow 3000
sudo ufw allow 3001

# 检查服务
pm2 logs
```

### 问题 2: 数据库连接失败
```bash
# 重启数据库
sudo systemctl restart postgresql

# 测试连接
psql -U agentrix -d agentrix -h localhost
```

### 问题 3: 内存不足
```bash
# 查看内存
free -h

# 重启服务
pm2 restart all
```

---

## 📞 需要帮助？

- 📖 详细教程: `部署到腾讯云完整教程.md`
- 🔧 故障排查: 见完整教程第 6 章
- 💬 GitHub Issues: https://github.com/你的用户名/agentrix-website/issues

---

**祝部署顺利！🎊**
